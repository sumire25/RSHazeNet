Modify this architecture so it uses a VLM model to caption the level of haziness of the images and uses that information
to get a better performance.
Furthermore modify also so it could continue training over a saved checkpoint weight file
